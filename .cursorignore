# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)

# 以下のファイルはプロジェクトで積極的に使用し、除外しないでください
!.env.local
!.env.sample
!.env.example

# 実際に除外するファイルやディレクトリ
node_modules/
dist/
build/
.git/
